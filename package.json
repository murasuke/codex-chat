{
  "name": "help-chatbot-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev"
  },
  "devDependencies": {
    "@types/pg": "^8.18.0"
  }
}
